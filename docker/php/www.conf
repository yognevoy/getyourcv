; Pool tuning override for the default www.conf shipped in php:8.4-fpm-alpine.
; Sized for the target VM (2 vCPU / 2GB RAM)
[www]
pm = dynamic
pm.max_children = 10
pm.start_servers = 3
pm.min_spare_servers = 2
pm.max_spare_servers = 6
pm.max_requests = 500
